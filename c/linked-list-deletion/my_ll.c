#include "my_ll.h"

int size(node *curr) {

}

node *delete_middle() {
    
}

void print(node *curr) {
    printf("\nLinked list datums:\n\n");
    while(curr != NULL) {
        printf("%d\n", curr->datum);
        curr = curr->next;
    }
}
